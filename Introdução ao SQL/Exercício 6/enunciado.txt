6. O operador SQL like diferencia maiúsculas de minúsculas (na maioria dos sistemas), mas a função lower( ) sobre strings pode ser usada para realizar uma combinação que não diferencia maiúsculas de minúsculas. Para mostrar como, escreva uma consulta que ache os departamentos cujos nomes contêm a string “sci” como uma substring, independentemente de o tipo de letra ser maiúscula ou minúscula.